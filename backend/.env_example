# Ejemplo de configuración de variables de entorno para el backend

# === SMTP (Correo de contacto) ===
# Servidor SMTP (por ejemplo, Gmail: smtp.gmail.com, Outlook: smtp.office365.com)
SMTP_HOST=smtp.gmail.com
# Puerto SMTP (Gmail: 465 para SSL, 587 para TLS)
SMTP_PORT=465
# Usuario de la cuenta de correo (correo completo)
SMTP_USER=tu_email@gmail.com
# Contraseña o contraseña de aplicación (NO tu contraseña normal, usa una contraseña de aplicación si tienes 2FA)
SMTP_PASS=tu_contraseña_o_app_password
# Correo de destino donde llegarán los mensajes del formulario de contacto
EMAIL_TO=destino@tucorreo.com

# === Base de datos ===
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_USERNAME=postgres
DATABASE_PASSWORD=tu_password
DATABASE_NAME=nombre_base_de_datos

# === JWT (Tokens de autenticación) ===
# Clave secreta para firmar JWT
JWT_SECRET=pon_aqui_una_clave_secreta_segura
# Clave secreta para refresh tokens
JWT_REFRESH_SECRET=pon_aqui_otra_clave_secreta
# Tiempo de expiración del access token (ej: 15m, 1h)
JWT_ACCESS_EXPIRES=15m
# Tiempo de expiración del refresh token (ej: 7d)
JWT_REFRESH_EXPIRES=7d

# === Otros ajustes del sistema ===
# Agrega aquí otras variables necesarias para tu entorno
